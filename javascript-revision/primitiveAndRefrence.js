//objects are refrence  datatype their adress is stored when
// arrays are also objects so they are also refrence datatype
// refrence datatype are mutable as only a refrence is tored in variable
// primitive are not mutable u r not modifying them u r  creating a new variable to sdtore them
let msg="HI";
 msg="Hello";

 arr=[1,2,3,4,5]
 arr.push(6);
 console.log(arr);
